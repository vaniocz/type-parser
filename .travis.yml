language: php

php:
    - 7.0

install:
    - php -n composer require satooshi/php-coveralls
  
before_script:
    - php -n composer self-update
    - php -n composer update
    - mkdir -p build/logs
    
script:
    - phpunit --coverage-clover build/logs/clover.xml

after_success:
    - travis_retry php vendor/bin/coveralls
